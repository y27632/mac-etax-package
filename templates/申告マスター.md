---
title: 申告基本情報
description: 全別表に共通する法人情報・申告情報の入力ファイル
---

# 申告基本情報

このファイルに会社情報・申告情報を記入してください。
記入後、このファイルをAIに渡すと各別表ファイルの会社情報欄を自動補完できます（下部のプロンプト参照）。

---

## 法人情報

| 項目 | 値 |
|------|----|
| 法人名 | |
| 法人名（フリガナ） | |
| 法人番号（13桁） | |
| 代表者氏名 | |
| 代表者氏名（フリガナ） | |
| 代表者住所 | |
| 納税地（法人所在地） | |
| 電話番号 | |
| 資本金（円） | |
| 事業種目 | |
| 法人区分 | （例: 普通法人 / 合同会社） |

---

## e-Tax情報

| 項目 | 値 |
|------|----|
| 利用者識別番号（16桁） | |
| 税務署コード（5桁） | |
| 税務署名 | |
| 手続きコードバージョン (proc_vr) | （例: `25.0.2`。KNOWHOW.md のバージョン対応表を参照） |

---

## 申告情報

| 項目 | 値 |
|------|----|
| 事業年度（自） | 令和　年　月　日 |
| 事業年度（至） | 令和　年　月　日 |
| 提出年月日 | 令和　年　月　日 |
| 申告区分 | （確定 / 修正 / 期限後） |
| 申告方式 | （青色 / 白色） |
| 決算確定の日 | 令和　年　月　日 |

> 日付はそのまま「令和X年Y月Z日」形式で記入してください。AIが内部形式（例: `"5,08,04,10"`）に変換します。

---

## 提出する別表（必要なものにチェック）

### 法人税（国税）

- [ ] 別表一　　　　　　　　※ほぼ必須
- [ ] 別表一（次葉）　　　　※ほぼ必須
- [ ] 別表二　　　　　　　　※ほぼ必須
- [ ] 別表四　　　　　　　　※ほぼ必須
- [ ] 別表五（一）　　　　　※ほぼ必須
- [ ] 別表五（二）　　　　　※ほぼ必須
- [ ] 別表六（一）　　　　　※受取利息など源泉所得税の控除がある場合
- [ ] 別表七（一）　　　　　※繰越欠損金がある場合（青色申告のみ）
- [ ] 別表十五　　　　　　　※交際費等がある場合
- [ ] 法人事業概況説明書　　※ほぼ必須

---

## メモ・特記事項

（申告に関して特記しておきたいことがあれば自由記述）

---

> **⚠️ 以下はAIが各別表ファイルに会社情報を補完する際に参照するプロンプトです（Step 1b）。内容を変更しないでください。**

---

## Step 1b: テンプレート補完プロンプト（AIへ）

上記の「申告基本情報」をもとに、チェックが入っている別表の各ファイル（`別表一.md` 等）の会社情報欄を埋めてください。

### 補完ルール

- チェック（`[x]`）が入っている別表のみ対象とする
- 各別表ファイルの「会社情報」セクションにある空欄を、上記の情報で埋める
- 財務数値（所得金額・税額・各勘定科目の金額など）は空欄のままにする
- 各別表ファイルの frontmatter の `xtx_version` は、バージョン対応表を参照して記入する
- 日付フォーマット: 元号コード付き文字列（例: 令和8年4月10日 → `"5,08,04,10"`）

### 法人情報の対応マッピング

| 別表の項目 | 申告基本情報の対応項目 |
|------------|----------------------|
| 法人名 | 法人名 |
| 法人名（フリガナ） | 法人名（フリガナ） |
| 法人番号 | 法人番号 |
| 代表者氏名 | 代表者氏名 |
| 代表者氏名（フリガナ） | 代表者氏名（フリガナ） |
| 代表者住所 | 代表者住所 |
| 電話番号 | 電話番号 |
| 納税地・法人住所 | 納税地 |
| 資本金 | 資本金 |
| 事業種目・事業の内容 | 事業種目 |
| 税務署コード＋名称 | 税務署コード ＋ 税務署名 |
| 事業年度（自） | 事業年度（自） |
| 事業年度（至） | 事業年度（至） |
| 提出年月日 | 提出年月日 |
| 決算確定の日 | 決算確定の日 |

### 出力形式

各別表ファイルの補完後の内容を、ファイル名を見出しにして出力してください。
AIに貼り付けてそのまま各ファイルに保存できる形式にしてください。

---

> **⚠️ 以下はAIがXTXファイルを生成する際に参照するプロンプトです（Step 3）。内容を変更しないでください。**

---

## Step 3: XTX生成プロンプト（AIへ）

このファイル（申告マスター）と、チェックが入っている各別表ファイルの記入値をもとに、
**自己完結した実行可能Pythonスクリプト**を1本出力してください。

ユーザーはそのスクリプトを `申告.py` として保存し、`python3 申告.py` を実行するだけで `.xtx` ファイルが生成されます。外部ライブラリは不要です。

### 手順

1. チェックが入っている別表ファイルをすべて読み込む
2. 各別表のXTX生成プロンプト（ファイル下部）に従い、Python辞書を組み立てる
3. 下記の `it_info マッピング` に従い、申告マスターの値を変換する
4. 下記の `ビルダーコード` と組み立てた辞書を合体させ、完全なスクリプトを出力する

### it_info マッピング

| it_info キー | 申告マスターの項目 | 変換ルール |
|---|---|---|
| `zeimusho_cd` | 税務署コード | 文字列（例: `"01107"`） |
| `zeimusho_nm` | 税務署名 | 文字列 |
| `nozeisha_id` | 利用者識別番号 | 文字列（16桁） |
| `nozeisha_bango` | 法人番号 | 文字列（13桁） |
| `nozeisha_nm_kn` | 法人名（フリガナ） | 全角カナ文字列 |
| `nozeisha_nm` | 法人名 | 文字列 |
| `nozeisha_adr` | 納税地 | 文字列 |
| `nozeisha_tel` | 電話番号 | `"XX-XXXX-XXXX"` 形式（ハイフン区切り） |
| `shihon_kin` | 資本金 | 整数（円） |
| `jigyo_naiyo` | 事業種目 | 文字列 |
| `daihyo_nm_kn` | 代表者氏名（フリガナ） | 全角カナ文字列 |
| `daihyo_nm` | 代表者氏名 | 文字列 |
| `daihyo_adr` | 代表者住所 | 文字列 |
| `teisyutsu_day` | 提出年月日 | `"元号,yy,mm,dd"` 形式（令和=5、例: 令和8年4月10日 → `"5,08,04,10"`） |
| `nendo_from` | 事業年度（自） | 同上 |
| `nendo_to` | 事業年度（至） | 同上 |

### 別表の追加順序

チェックが入っている別表を以下の順序で `doc.add_form()` に追加する。フォームIDとバージョンは各別表ファイルの frontmatter（`xtx_form` / `xtx_version`）を参照する。

> **⚠️ 重要**: この順序は e-Tax の XSD（RHO0022-240.xsd）の sequence 定義に従う必要がある。順序を間違えると「XML構造チェックエラー」になる。

1. 別表一（HOA112）　　　　　※必須（minOccurs=1）
2. 別表一（次葉）（HOA116）　※ほぼ必須
3. 別表二（HOA201）
4. 別表四（HOA420）
5. 別表五（一）（HOA511）　　※チェックがある場合のみ
6. 別表五（二）（HOA522）　　※チェックがある場合のみ
7. 別表六（一）（HOB016）　　※源泉所得税の控除がある場合のみ
8. 別表七（一）（HOB710）　　※繰越欠損金がある場合のみ
9. 別表十五（HOE200）　　　　※チェックがある場合のみ
10. 法人事業概況説明書（HOK010）

### ビルダーコード（出力スクリプトの冒頭にそのまま含めること）

```python
import json
import os
import re
from datetime import datetime
from xml.sax.saxutils import escape as xml_escape

_JNENGAPPI_RE = re.compile(r'^(\d),(\d{2}),(\d{2}),(\d{2})$')

N_HOJIN = "http://xml.e-tax.nta.go.jp/XSD/hojin"
N_GEN   = "http://xml.e-tax.nta.go.jp/XSD/general"
N_RDF   = "http://www.w3.org/1999/02/22-rdf-syntax-ns#"
N_DSIG  = "http://www.w3.org/2000/09/xmldsig#"

class ETaxDocument:
    def __init__(self, procedure_cd, proc_vr, it_info):
        self.procedure_cd = procedure_cd
        self.proc_vr      = proc_vr
        self.it_info      = it_info
        self.forms        = []

    def add_form(self, form_id, vr, contents_dict):
        self.forms.append({"id": form_id, "vr": vr, "data": contents_dict})

    def _dict_to_xml_wrapper(self, form_id, vr, d):
        indent = "      "
        xml = [f'{indent}<{form_id} id="{form_id}" VR="{vr}" softNM="EveryScience-xtx-v2" sakuseiNM="EveryScience-xtx-v2" sakuseiDay="{datetime.now().strftime("%Y-%m-%d")}">']
        for k, v in d.items():
            if v is not None:
                xml.append(self._dict_to_xml_real(k, v, indent + "  "))
        xml.append(f"{indent}</{form_id}>")
        return "\n".join(xml)

    def _dict_to_xml_real(self, tag_name, d, indent):
        xml = []
        if isinstance(d, dict):
            if not d:
                xml.append(f"{indent}<{tag_name}/>")
            else:
                xml.append(f"{indent}<{tag_name}>")
                for k, v in d.items():
                    if v is not None:
                        xml.append(self._dict_to_xml_real(k, v, indent + "  "))
                xml.append(f"{indent}</{tag_name}>")
        elif isinstance(d, list):
            for el in d:
                if el is not None:
                    xml.append(self._dict_to_xml_real(tag_name, el, indent))
        else:
            if isinstance(d, str) and d.startswith("REF:"):
                xml.append(f'{indent}<{tag_name} IDREF="{d[4:]}"/>')
            elif d == "":
                xml.append(f"{indent}<{tag_name}/>")
            else:
                m = _JNENGAPPI_RE.match(str(d))
                if m:
                    era, yy, mm, dd = m.groups()
                    inner = f'<gen:era>{era}</gen:era><gen:yy>{yy}</gen:yy><gen:mm>{mm}</gen:mm><gen:dd>{dd}</gen:dd>'
                    xml.append(f"{indent}<{tag_name}>{inner}</{tag_name}>")
                else:
                    xml.append(f"{indent}<{tag_name}>{xml_escape(str(d))}</{tag_name}>")
        return "\n".join(xml)

    def _build_it_header(self):
        it_xml = ['      <IT id="IT" VR="1.5">']
        def add(tag, xmlstring):
            if xmlstring:
                it_xml.append(f'        <{tag} ID="{tag}">{xmlstring}</{tag}>')
        info = self.it_info
        zeimusho_inner = f'<gen:zeimusho_CD>{xml_escape(str(info.get("zeimusho_cd", "")))}</gen:zeimusho_CD>'
        if info.get("zeimusho_nm"):
            zeimusho_inner += f'<gen:zeimusho_NM>{xml_escape(info["zeimusho_nm"])}</gen:zeimusho_NM>'
        add("ZEIMUSHO", zeimusho_inner)
        if info.get("teisyutsu_day"):
            y, yy, mm, dd = info["teisyutsu_day"].split(",")
            add("TEISYUTSU_DAY", f'<gen:era>{y}</gen:era><gen:yy>{yy}</gen:yy><gen:mm>{mm}</gen:mm><gen:dd>{dd}</gen:dd>')
        add("NOZEISHA_ID", info.get("nozeisha_id"))
        if info.get("nozeisha_bango"):
            add("NOZEISHA_BANGO", f'<gen:hojinbango>{xml_escape(info["nozeisha_bango"])}</gen:hojinbango>')
        if info.get("nozeisha_nm_kn"):
            add("NOZEISHA_NM_KN", xml_escape(info["nozeisha_nm_kn"]))
        if info.get("nozeisha_nm"):
            add("NOZEISHA_NM", xml_escape(info["nozeisha_nm"]))
        if info.get("nozeisha_adr"):
            add("NOZEISHA_ADR", xml_escape(info["nozeisha_adr"]))
        if info.get("nozeisha_tel"):
            t1, t2, t3 = info["nozeisha_tel"].split("-")
            add("NOZEISHA_TEL", f'<gen:tel1>{t1}</gen:tel1><gen:tel2>{t2}</gen:tel2><gen:tel3>{t3}</gen:tel3>')
        if info.get("shihon_kin") is not None:
            add("SHIHON_KIN", str(info["shihon_kin"]))
        if info.get("jigyo_naiyo"):
            add("JIGYO_NAIYO", xml_escape(info["jigyo_naiyo"]))
        if info.get("daihyo_nm_kn"):
            add("DAIHYO_NM_KN", xml_escape(info["daihyo_nm_kn"]))
        if info.get("daihyo_nm"):
            add("DAIHYO_NM", xml_escape(info["daihyo_nm"]))
        if info.get("daihyo_adr"):
            add("DAIHYO_ADR", xml_escape(info["daihyo_adr"]))
        add("TETSUZUKI", f'<procedure_CD>{self.procedure_cd}</procedure_CD>')
        if info.get("nendo_from"):
            y, yy, mm, dd = info["nendo_from"].split(",")
            add("JIGYO_NENDO_FROM", f'<gen:era>{y}</gen:era><gen:yy>{yy}</gen:yy><gen:mm>{mm}</gen:mm><gen:dd>{dd}</gen:dd>')
        if info.get("nendo_to"):
            y, yy, mm, dd = info["nendo_to"].split(",")
            add("JIGYO_NENDO_TO", f'<gen:era>{y}</gen:era><gen:yy>{yy}</gen:yy><gen:mm>{mm}</gen:mm><gen:dd>{dd}</gen:dd>')
        it_xml.append("      </IT>")
        return "\n".join(it_xml)

    def generate(self, output_path):
        xml = [
            '<?xml version="1.0" encoding="UTF-8" standalone="no"?>',
            f'<DATA xmlns="{N_HOJIN}" xmlns:gen="{N_GEN}" xmlns:rdf="{N_RDF}" xmlns:dsig="{N_DSIG}">',
            f'  <{self.procedure_cd} id="{self.procedure_cd}" VR="{self.proc_vr}">',
            '    <CATALOG id="CATALOG">',
            '      <rdf:RDF>',
            '        <rdf:Description id="REPORT">',
            '          <SEND_DATA/>',
            '          <IT_SEC><rdf:Description about="#IT"/></IT_SEC>',
            '          <FORM_SEC><rdf:Seq>',
        ]
        for f in self.forms:
            xml.append(f'              <rdf:li><rdf:Description about="#{f["id"]}"/></rdf:li>')
        xml += [
            '          </rdf:Seq></FORM_SEC>',
            '          <TENPU_SEC/><XBRL_SEC/><XBRL2_1_SEC/><SOFUSHO_SEC/><ATTACH_SEC/><CSV_SEC/>',
            '        </rdf:Description>',
            '      </rdf:RDF>',
            '    </CATALOG>',
            '    <CONTENTS id="CONTENTS">',
        ]
        xml.append(self._build_it_header())
        for f in self.forms:
            xml.append(self._dict_to_xml_wrapper(f["id"], f["vr"], f["data"]))
        xml += [
            '    </CONTENTS>',
            f'  </{self.procedure_cd}>',
            '</DATA>',
        ]
        with open(output_path, "w", encoding="utf-8") as file:
            file.write("\n".join(xml))
```

### 出力スクリプトの構成

以下の構成で1つのPythonスクリプトを出力する。

```python
# 自動生成スクリプト: python3 申告.py で実行してください

# ===== ビルダーコード（変更不要）=====
[上記ビルダーコードをそのままコピー]

# ===== 申告データ =====
it_info = {
    "zeimusho_cd":   "<税務署コード>",
    "zeimusho_nm":   "<税務署名>",
    "nozeisha_id":   "<利用者識別番号>",
    "nozeisha_bango":"<法人番号>",
    "nozeisha_nm_kn":"<法人名フリガナ>",
    "nozeisha_nm":   "<法人名>",
    "nozeisha_adr":  "<納税地>",
    "nozeisha_tel":  "<電話番号>",
    "shihon_kin":    <資本金（整数）>,
    "jigyo_naiyo":   "<事業種目>",
    "daihyo_nm_kn":  "<代表者フリガナ>",
    "daihyo_nm":     "<代表者氏名>",
    "daihyo_adr":    "<代表者住所>",
    "teisyutsu_day": "<提出年月日>",
    "nendo_from":    "<事業年度（自）>",
    "nendo_to":      "<事業年度（至）>",
}

doc = ETaxDocument("RHO0022", "<proc_vr>", it_info)

# 各別表を追加（チェックが入っているもののみ・上記の順序で）
doc.add_form("<form_id>", "<vr>", {
    # 各別表のXTX生成プロンプトが出力する辞書
})
# ... 別表の数だけ繰り返す

doc.generate("申告.xtx")
print("生成完了: 申告.xtx")
```

---
